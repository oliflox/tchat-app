<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="style.css">
  <title>Tchat</title>
</head>
<body>
  <!-- menu -->
  <div id="nav">
    <div>
      <a href="/"><button>Publication</button></a>
      <a href="/chat"><button>Tchat</button></a>
      <a href="/profile"><button>Profil</button></a>
    </div>
    
    <form method="POST" action="/logout">
      <button type="submit">Déconnexion</button>
    </form>
  </div>
  <!-- fin menu -->
  
  <h1>Publication</h1>
  <div id="connexion-status"></div>

  <div id="post-list">
  <% for (var i = 0; i < posts.length; i++ ) { %>
    <div class="post">
      <div class="post-head">
        <b><%= posts[i].user.name %></b> 
        <span><%= posts[i].formatedDate %></span>
      </div>
      <p><%= posts[i].content %></p>
    </div>
  <% } %>
  </div>

  <script src="cookies.js"></script>
  <script src="clientPost.js"></script>
</body>
</html>